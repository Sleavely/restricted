{
  "name": "restricted",
  "version": "0.0.2",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "postversion": "git push $(git config --local branch.$(git symbolic-ref --short HEAD).remote) $(git describe --abbrev=0)",
    "test": "mocha"
  },
  "keywords": [
    "acl",
    "access control",
    "permissions"
  ],
  "author": "Joakim Hedlund",
  "license": "MIT",
  "dependencies": {
    "lodash.get": "^4.4.2",
    "lodash.set": "^4.3.2",
    "lodash.unset": "^4.5.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0"
  }
}
